import 'package:flutter/material.dart';

class LoadingJobs extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: <Widget>[
        Container(
          margin: EdgeInsets.only(bottom: 15.0),
          decoration: BoxDecoration(
            border: Border.all(color: Color(0xffe9ecf5)),
            borderRadius: BorderRadius.circular(10.0),
            color: Colors.white,
          ),
          width: double.infinity,
          height: 150.0,
        ),
        Container(
          margin: EdgeInsets.only(bottom: 15.0),
          decoration: BoxDecoration(
            border: Border.all(color: Color(0xffe9ecf5)),
            borderRadius: BorderRadius.circular(10.0),
            color: Colors.white,
          ),
          width: double.infinity,
          height: 150.0,
        ),
        Container(
          margin: EdgeInsets.only(bottom: 15.0),
          decoration: BoxDecoration(
            border: Border.all(color: Color(0xffe9ecf5)),
            borderRadius: BorderRadius.circular(10.0),
            color: Colors.white,
          ),
          width: double.infinity,
          height: 150.0,
        ),
        Container(
          margin: EdgeInsets.only(bottom: 15.0),
          decoration: BoxDecoration(
            border: Border.all(color: Color(0xffe9ecf5)),
            borderRadius: BorderRadius.circular(10.0),
            color: Colors.white,
          ),
          width: double.infinity,
          height: 150.0,
        )
      ],
    );
  }
}
